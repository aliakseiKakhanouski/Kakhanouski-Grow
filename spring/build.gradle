apply plugin: 'java'

sourceCompatibility = "${javaVersion}"

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    compile "org.springframework:spring-context:${springContextVersion}"
}
